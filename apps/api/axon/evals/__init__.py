"""Prompt evaluation harnesses.

You cannot iterate on what you cannot measure (roadmap T2.1): these
harnesses exist BEFORE the prompts they evaluate, so every prompt change in
T2.2+ is judged against fixed, hand-written gold fixtures instead of vibes.
"""
