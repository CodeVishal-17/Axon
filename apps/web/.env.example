# Copy to `.env.local`. NEXT_PUBLIC_* variables are baked into the browser
# bundle at build time — never put secrets here.

# Base URL of the Axon API (FastAPI backend).
NEXT_PUBLIC_API_URL=http://localhost:8000
